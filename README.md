#### VIM THEME
#####USAGE
```shell
git clone https://github.com/cfmcgrady/vim-theme.git
git submodule update --init --recursive
```
or
```shell
git clone --recursive https://github.com/cfmcgrady/vim-theme.git
```
